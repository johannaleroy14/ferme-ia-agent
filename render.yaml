services:
  - type: web
    name: ferme-ia-agent
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python test_notification.py"
    plan: free
    region: oregon
    branch: main
